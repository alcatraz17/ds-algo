class M {
  moduloInverse(n, m) {
    const n = ((n % m) + m) % m;

    let [old_r, r] = [n, m];
    let [old_s, s] = [1, 0];

    while (r !== 0) {}
  }
}

const m = new M();
const modInv = console.log(m.moduloInverse(10, 17));
